% Eloundou et al. (2024): task-level LLM exposure
% Split into core (measure definition + code chain + pharmacists example)
% and extra (task-level scoring table + other one-to-one mappings) so the
% presentation deck can input only the core frames.

\input{eloundou_core}
\input{eloundou_extra}
